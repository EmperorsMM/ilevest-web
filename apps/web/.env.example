# Local dev env for the web app. Copy to .env.local and fill from the team secret store.
# See the repository-root .env.example for the canonical, documented list. NEVER commit .env.local.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
